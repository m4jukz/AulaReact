export default function Exercicio3 ({prestação,juros,atraso})
{
    let resultado=( (valor)+(valor)*(taxa)*(taxa)/100*(tempo) )
    return(
        <div>
            <p>O valor da taxa é {resultado}</p>
        </div>
    );
}